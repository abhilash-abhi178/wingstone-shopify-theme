# Bug Report — wingstone-shopify-theme

Scan date: 2026-06-01

Summary:
- Performed repository-wide scan for common bug patterns (Liquid numeric comparisons, TODO/FIXME markers, JS error throws/console.error).

Findings (high priority):

- Liquid numeric comparison errors:
  - Symptom reported: "Liquid error (sections/header line 56): comparison of String with 0 failed".
  - Cause: several templates compared `.products_count` directly to `0`; in some contexts that value can be a string or missing, causing Liquid to fail the numeric comparison.
  - Fixes applied:
    - Updated `sections/header.liquid` to coerce `.products_count` to a number before comparing (used `| plus: 0`). See [sections/header.liquid](sections/header.liquid) for changes.
    - Updated `sections/featured-collection.liquid` similarly. See [sections/featured-collection.liquid](sections/featured-collection.liquid).

Other notable findings (recommend review):

- JavaScript error handling and throws (informational — not changed):
  - `assets/cart-drawer.js`: calls to `console.error` and `throw new Error('Cart add failed')` — review for user-facing messages and analytics.
    See [assets/cart-drawer.js](assets/cart-drawer.js).
  - `assets/predictive-search.js`: `throw error` occurrences — ensure callers handle these rejects. See [assets/predictive-search.js](assets/predictive-search.js).
  - Several other `console.error` usages: `assets/product-form.js`, `assets/cart.js`, `assets/product-info.js` — review for silent failures.

- TODO/FIXME markers:
  - `sections/header.liquid` contains a `<!-- FIX: Badge starts hidden, JS shows it when count > 0 -->` comment. Consider adding a server-side fallback or ARIA live region for accessibility.

Recommendations / Next steps:

1. Run a Liquid linting tool (shopify-theme-check) to catch other template issues automatically.
2. Run a JS linter (ESLint) across `assets/*.js` and add stricter error handling where `throw` is used without clear upstream handlers.
3. Review all direct numeric comparisons of dynamic values and coerce using `| plus: 0` (or check for `blank`) where appropriate.
4. Consider adding unit or integration tests for critical theme behaviors (cart updates, search) if you have a CI pipeline.

Files changed in this pass:
- [sections/header.liquid](sections/header.liquid)
- [sections/featured-collection.liquid](sections/featured-collection.liquid)
- [sections/collection-list.liquid](sections/collection-list.liquid)
- [sections/main-list-collections.liquid](sections/main-list-collections.liquid)

If you'd like, I can:
- Run a full `theme-check` (if you want, I can add instructions or run it locally if you have the tools installed).
- Apply automatic numeric coercions to other `.products_count` comparisons I found.
- Tidy up JS error handling (wrap throws with try/catch where appropriate) and run ESLint.

CI / Automation:

- Added a GitHub Actions workflow: [.github/workflows/theme-check.yml](.github/workflows/theme-check.yml) to run `theme-check` on pushes and PRs to `main`.

  - Added ESLint config and workflow to lint JavaScript in `assets/`:
    - `.eslintrc.json` with `eslint:recommended` rules.
    - `.github/workflows/eslint.yml` to run ESLint on pushes and PRs.

Additional template hardening:
- Coerced several dynamic values to numeric and added presence guards to avoid Liquid type errors:
  - `sections/main-product.liquid`: coerced `inventory_quantity` into numeric `inv`.
  - `sections/header.liquid`: coerced `cart.item_count` before comparison.
  - `sections/main-cart-items.liquid`: coerced `cart.item_count` and `cart.total_discount` before comparisons.
  - `sections/search-results.liquid`: coerced `search.results_count` and compared numerically to `search.results.size`.
  - `sections/footer.liquid`: added presence guards for `linklists[...]` and coerced `.links.size`.


---
Generated by GitHub Copilot assistant.
