<section class="section">
  <div class="page-width product">
    <div class="product__media">
      {% if product.featured_media %}
        {{ product.featured_media | image_url: width: 1200 | image_tag: loading: 'eager', alt: product.title }}
      {% else %}
        {{ 'product-1' | placeholder_svg_tag }}
      {% endif %}
    </div>

    <div class="product__info">
      <p class="eyebrow">{{ product.vendor | default: shop.name }}</p>
      <h1 class="product__title">{{ product.title }}</h1>
      <div class="price">{{ product.price | money }}</div>

      {% if product.description != blank %}
        <div class="product__description">{{ product.description }}</div>
      {% endif %}

      {% form 'product', product, class: 'product-form' %}
        {% unless product.has_only_default_variant %}
          {% for option in product.options_with_values %}
            <label>
              <span class="eyebrow">{{ option.name }}</span>
              <select class="field" name="options[{{ option.name | escape }}]" data-option-index="{{ forloop.index }}">
                {% for value in option.values %}
                  <option value="{{ value | escape }}" {% if option.selected_value == value %}selected{% endif %}>{{ value }}</option>
                {% endfor %}
              </select>
            </label>
          {% endfor %}
        {% endunless %}

        <input type="hidden" name="id" value="{{ product.selected_or_first_available_variant.id }}">
        <label>
          <span class="eyebrow">Quantity</span>
          <input class="field" type="number" name="quantity" value="1" min="1">
        </label>
        <button class="button" type="submit" {% unless product.available %}disabled{% endunless %}>
          {% if product.available %}Add to cart{% else %}Sold out{% endif %}
        </button>
        {{ form | payment_button }}
        <script type="application/json" data-product-variants>{{ product.variants | json }}</script>
      {% endform %}
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Main product",
  "settings": []
}
{% endschema %}
